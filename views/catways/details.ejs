<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>
<body>
  <%- include('../partials/navbar') %>
  
  <div class="container mt-4">
    <%- include('../partials/messages') %>
    
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1><i class="bi bi-list"></i> <%= title %></h1>
      <a href="/dashboard/catways" class="btn btn-secondary">
        <i class="bi bi-arrow-left"></i> Retour à la liste
      </a>
    </div>

    <div class="card">
      <div class="card-header bg-primary text-white">
        <h4 class="mb-0">Catway <%= catway.catwayNumber %></h4>
      </div>
      <div class="card-body">
        <div class="row">
          <div class="col-md-6">
            <dl class="row">
              <dt class="col-sm-4">Numéro :</dt>
              <dd class="col-sm-8"><strong><%= catway.catwayNumber %></strong></dd>
              
              <dt class="col-sm-4">Type :</dt>
              <dd class="col-sm-8">
                <span class="badge bg-<%= catway.type === 'long' ? 'primary' : 'secondary' %>">
                  <%= catway.type %>
                </span>
              </dd>
              
              <dt class="col-sm-4">État :</dt>
              <dd class="col-sm-8"><%= catway.catwayState %></dd>
            </dl>
          </div>
          <div class="col-md-6">
            <dl class="row">
              <dt class="col-sm-4">Créé le :</dt>
              <dd class="col-sm-8"><%= new Date(catway.createdAt).toLocaleString('fr-FR') %></dd>
              
              <dt class="col-sm-4">Modifié le :</dt>
              <dd class="col-sm-8"><%= new Date(catway.updatedAt).toLocaleString('fr-FR') %></dd>
              
              <dt class="col-sm-4">ID :</dt>
              <dd class="col-sm-8"><code><%= catway._id %></code></dd>
            </dl>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
